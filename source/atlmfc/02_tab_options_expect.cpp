/*
    本文件的样本代码来自于astyle.html中的Tab Options部分

    atlmfc_astyle_options.txt中使用--indent=spaces=4，即用4个空格取代Tab作为缩进。
    C#默认也是使用4个空格作为缩进，说明微软也认可这种做法。
*/
void Foo()
{
    if (isBar1
        && isBar2)
    {
        bar();
    }
}
